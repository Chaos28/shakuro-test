export enum BREAKPOINTS {
  sm = 576,
  md = 768,
  lg = 992,
  xl = 1200,
}

export enum BREAKPOINTS_ORIENTATION {
  portraitWideX = 0.5,
  portraitWide = 0.61,
  portrait = 0.8,
  landscape = 1.2,
  landscapeWide = 1.7,
  landscapeWideX = 2,
}

export enum EVENTS_NAME {
  resize = 'resize',
}
